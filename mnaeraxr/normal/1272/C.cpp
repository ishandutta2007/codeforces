/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author marX
 */

#include <iostream>
#include <fstream>

#include <bits/stdc++.h>

using namespace std;

class CYetAnotherBrokenKeyboard {
public:
    void solve(std::istream &in, std::ostream &out) {
        int n, k;
        in >> n >> k;
        string s;
        in >> s;

        vector<int> ok(26);

        for (int i = 0; i < k; ++i) {
            char c;
            in >> c;
            ok[c - 'a'] = true;
        }

        long long answer = 0;
        long long cur = 0;

        for (int i = 0; i < n; ++i) {
            if (ok[s[i] - 'a']) {
                cur++;
            } else {
                answer += cur * (cur + 1) / 2;
                cur = 0;
            }
        }

        answer += cur * (cur + 1) / 2;
        out << answer << endl;
    }
};


int main() {
    CYetAnotherBrokenKeyboard solver;
    std::istream &in(std::cin);
    std::ostream &out(std::cout);
    solver.solve(in, out);
    return 0;
}