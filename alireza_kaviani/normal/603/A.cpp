/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author alireza_kaviani
 */

#include <bits/stdc++.h>
#include <ext/pb_ds/tree_policy.hpp>
#include <ext/pb_ds/assoc_container.hpp>

using namespace std;
using namespace __gnu_pbds;

template<class T> using Tree = tree<T, null_type, less<T>, rb_tree_tag, tree_order_statistics_node_update>;

typedef long long ll;
typedef long double ld;
typedef pair<ll, ll> pll;
typedef pair<double, double> pdd;

#define all(x)                      (x).begin(),(x).end()
#define Sort(x)                     sort(all((x)))
#define X                           first
#define Y                           second
#define sep                         ' '
#define endl                        '\n'
#define SZ(x)                       ll(x.size())

ll poww(ll a, ll b, ll md) {
    return (!b ? 1 : (b & 1 ? a * poww(a * a % md, b / 2, md) % md : poww(a * a % md, b / 2, md) % md));
}

const ll MAXN = 1e5 + 10;
const ll LOG = 22;
const ll INF = 8e18;
const ll MOD = 1e9 + 7; // 998244353; // 1e9 + 9;

ll n, dp[2][3][MAXN];
string s;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    cin >> n >> s;
    s = "." + s;

    for (ll i = 1; i <= n; i++) {
        ll x = s[i] - 48;
        for (ll j = 0; j < 3; j++) dp[1 - x][j][i] = dp[1 - x][j][i - 1];
        for (ll j = 0; j < 3; j++) dp[x][j][i] = max(dp[x][j][i - 1], dp[1 - x][j][i] + 1);
        for (ll j = 1; j < 3; j++) dp[x][j][i] = max(dp[x][j][i], dp[x][j - 1][i - 1] + 1);
        /*for(ll j = 0 ; j < 3 ; j++) cout << dp[0][j][i] << sep << dp[1][j][i] << endl;
        cout << "========" << endl;*/
    }
    cout << max(dp[0][2][n], dp[1][2][n]) << endl;

    return 0;
}
/*

*/