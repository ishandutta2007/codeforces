#include <bits/stdc++.h>

using namespace std;

int main() {
    int T;
    scanf("%d", &T);
    while (T--) {
        int a;
        scanf("%d", &a);
        printf("%d\n", 1 + a / 2);
    }
    return 0;
}