#include <bits/stdc++.h>

using namespace std;
int w, a, b, c, d;
int main()
{
    ios_base::sync_with_stdio(false);
    cin >> w;
    while (w--) {
        cin >> a >> b >> c >> d;
        cout << 1 << " " << 1 << " " << a << " " << b << "\n";
    }
    return 0;
}