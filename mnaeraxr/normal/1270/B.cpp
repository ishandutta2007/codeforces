/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author marX
 */

#include <iostream>
#include <fstream>

#include <bits/stdc++.h>

using namespace std;

class BInterestingSubarray {
public:
    void solve(std::istream &in, std::ostream &out) {
        int t;
        in >> t;

        while (t--) {
            int n;
            in >> n;

            vector<int> a(n);

            for (int i = 0; i < n; ++i) {
                in >> a[i];
            }

            int x = -1;

            for (int i = 0; i + 1 < n; ++i) {
                if (abs(a[i] - a[i + 1]) >= 2) {
                    x = i;
                    break;
                }
            }

            if (x == -1) {
                out << "NO\n";
            } else {
                out << "YES\n";
                out << x + 1 << " " << x + 2 << "\n";
            }
        }
    }
};


int main() {
    BInterestingSubarray solver;
    std::istream &in(std::cin);
    std::ostream &out(std::cout);
    solver.solve(in, out);
    return 0;
}