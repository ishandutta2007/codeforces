#pragma comment(linker, "/STACK:512000111")
#include <iostream>
#include <cstdio>
#include <vector>
#include <set>
#include <map>
#include <string>

using namespace std; 


void solve() {
	// answer
	int answer = 0;
	// input

	int n, m; cin >> n >> m;
	int l = 300;
	cout << string(l, '9') << endl;
	cout << string(l, '9') << string(l-1, '0') << 1 << endl;
}

int main() {
	int t; //cin >> t;
    t = 1;
	while (t-->0) solve();
	return 0;
}