#include <bits/stdc++.h>

using namespace std;

const int N = 1e5 + 10;

void solve() {
    int n;
    cin >> n;
    for (int i = 0; i < n; i++) {
        int x;
        cin >> x;
    }
    cout << 1 << ' ' << n << '\n';
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    int tc;
    cin >> tc;
    while (tc--) {
        solve();
    }
}