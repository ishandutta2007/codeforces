#include <bits/stdc++.h>
#define pii pair<int,int>
using namespace std;
typedef long long ll;
typedef long double ld;

int n,a;

int main()
{
    cin >> n;
    while (n--) {
        cin >> a;
        if (a == 1) { cout << -1; return 0; }
    }
    cout << 1;

    return 0;
}