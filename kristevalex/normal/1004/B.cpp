#include <bits/stdc++.h>
using namespace std;

#define FAST ios_base::sync_with_stdio(false); \
			 cin.tie(nullptr);                 \
	         cout.tie(nullptr)


long long n, m, k, ans;


int main()
    {
    FAST;

    cin >> n >> m;

    //vector<type> data (n);


    for (int i = 0; i < m; i++)
        {
        cin >> k >> k;
        }


    for (int i = 0; i < n; i++)
        {
        cout << i%2;
        }

    return 0;
    }