/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author marX
 */

#include <iostream>
#include <fstream>

#include <bits/stdc++.h>

using namespace std;

int dx[4] = {+1, -1, 0, 0};
int dy[4] = {0, 0, +1, -1};

class CShawarmaTent {
public:
    void solve(std::istream &in, std::ostream &out) {
        int n, sx, sy;
        in >> n >> sx >> sy;

        vector<int> total(4);

        for (int i = 0; i < n; ++i) {
            int x, y;
            in >> x >> y;

            if (x > sx) total[0]++;
            if (x < sx) total[1]++;
            if (y > sy) total[2]++;
            if (y < sy) total[3]++;
        }

        int p = max_element(total.begin(), total.end()) - total.begin();

        out << total[p] << endl;
        out << sx + dx[p] << " " << sy + dy[p] << endl;
    }
};


int main() {
    CShawarmaTent solver;
    std::istream &in(std::cin);
    std::ostream &out(std::cout);
    solver.solve(in, out);
    return 0;
}