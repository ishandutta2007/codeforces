#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    cerr.tie(nullptr);

    double n;
    long long t;
    cin>>n>>t;
    printf("%.20f",n*pow(1.000000011,t));
    return 0;
}