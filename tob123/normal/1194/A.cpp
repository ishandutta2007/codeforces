#include <bits/stdc++.h>
using namespace std;
#define sz(x) ((int) (x).size())

typedef long long ll;

ll T, N, X;

int main(){
  cin >> T;
  while(T--){
    cin >> N >> X;
    cout << X*2 << endl;
  }

  return 0;
}