#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define array a

int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	
	int n;
	string s;
	cin >> n >> s;
	sort(s.begin(), s.end());
	cout << s;
}