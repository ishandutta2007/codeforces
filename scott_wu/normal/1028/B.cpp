#include <iostream>
#include <iomanip>
#include <cstdio>
#include <vector>
#include <string>
#include <map>
#include <algorithm>
#include <set>

using namespace std;
typedef long long ll;

int main()
{
    ios_base::sync_with_stdio(0);
    int N, M;
    cin >> N >> M;
    for (int i = 0; i < 1000; i++)
    {
        cout << 4;
    }
    cout << "\n";
    for (int i = 0; i < 1000; i++)
    {
        if (i == 999) cout << 6;
        else cout << 5;
    }
    cout << "\n";
}