#include <bits/stdc++.h>
#define pb emplace_back
#define mp make_pair

using namespace std;

int32_t main()
{
	ios::sync_with_stdio(false); cin.tie(NULL); cout.precision(16);
	long double n; cin >> n;
	long double o = 1.000000011;
	int t; cin >> t;
	long double p = n * pow(o, t);
	cout << p << "\n";
}