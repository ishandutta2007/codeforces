#include <bits/stdc++.h>

using namespace std;

int main() {
	int tc; scanf("%d", &tc);
	while(tc--) {
		int N, M, x, y; scanf("%d%d%d%d", &N, &M, &x, &y);
		printf("%d %d %d %d\n", 1, 1, N, M);
	}
	return 0;
}