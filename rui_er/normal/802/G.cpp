#include <bits/stdc++.h>
using namespace std;

int main()
{
    string s;
    cin>>s;
    if(s.find('i', s.find('d', s.find('i', s.find('e', s.find('h'))))) == string::npos) cout<<"NO"<<endl;
    else cout<<"YES"<<endl;
    return 0;
}