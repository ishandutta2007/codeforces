#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main(){
    ios_base::sync_with_stdio(false),cin.tie(0);
    cout.precision(10);
    cout<<fixed;

    int n;
    cin >> n;
    if(n%2 != 0){cout << "Ehab"; return 0;}
    cout << "Mahmoud";
    return 0;
}