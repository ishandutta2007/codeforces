#include <bits/stdc++.h>

typedef long long ll;
using namespace std;

int main(){
    int t;
    cin.tie(NULL);
    ios_base::sync_with_stdio(false);
    cin >> t;
    while(t--){
        string n;
        cin >> n;
        int ans=(int)n.size();
        cout << ans << '\n';
    }
    return 0;
}