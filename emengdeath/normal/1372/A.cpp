#include <algorithm>
#include <cstdio>
using namespace std;
int main() {
    int T;
    scanf("%d", &T);
    while (T --) {
        int n;
        scanf("%d", &n);
        while (n --) {
            printf("1 ");
        }
        puts("");
    }
    return 0;
}