/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author marX
 */

#include <iostream>
#include <fstream>

#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

class AThreeFriends {
public:
    void solve(std::istream &in, std::ostream &out) {
        int n;
        in >> n;

        while (n-- > 0) {
            int a, b, c;
            in >> a >> b >> c;

            vector<int> l = {a, b, c};
            sort(l.begin(), l.end());

            a = l[0], b = l[1], c = l[2];

            if (a < c) ++a;
            if (b < c) ++b;
            if (b > a) --b;
            if (c > a) --c;

            out << 2 * (c - a) << endl;
        }
    }
};


int main() {
    AThreeFriends solver;
    std::istream &in(std::cin);
    std::ostream &out(std::cout);
    solver.solve(in, out);
    return 0;
}