#include <bits/stdc++.h>
using namespace std;
#define sz(x) ((int) (x).size())

typedef long long ll;
typedef pair<int,int> pii;

int K;

int main(){
  ios_base::sync_with_stdio(false);
  cin.tie(0);
  cin >> K;
  cout << 3 << " " << 3 << endl;
  cout << (1<<18)-1 << " " << (1<<17) << " " << 0 << endl;
  cout << (1<<17)-1 << " " << (1<<18)-1 << " " << (1<<17)-1 << endl;
  cout << 0 << " " << 0 << " " << K << endl;

  return 0;
}