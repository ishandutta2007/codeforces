#include <iostream>
#include <vector>
#include <string>
#include <map>
#include <set>
#include <cmath>
#include <algorithm>

using namespace std;

const int INF = 1e9;

typedef long long ll;

int main() {
	int s, n;
	cin >> n >> s;
	cout << (s + n - 1) / n << endl;
	//system("pause");
	return 0;
}