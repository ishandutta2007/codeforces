/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author marX
 */

#include <iostream>
#include <fstream>

#include <bits/stdc++.h>

using namespace std;

class BAAndB {
public:
    void solve(std::istream &in, std::ostream &out) {
        int t;
        in >> t;

        while (t--) {
            int a, b;
            in >> a >> b;

            int c = abs(a - b);
            int k = 0;

            while (k * (k + 1) / 2 < c || (k * (k + 1) / 2 + c) % 2 != 0) {
                ++k;
            }

            out << k << endl;
        }
    }
};


int main() {
    BAAndB solver;
    std::istream &in(std::cin);
    std::ostream &out(std::cout);
    solver.solve(in, out);
    return 0;
}