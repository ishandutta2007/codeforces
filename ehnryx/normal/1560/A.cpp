// https://codeforces.com/contest/1560/problem/A
#include <random>
#include <bits/stdc++.h>
int main() {
  scanf("%s", (char*)malloc(9 * sizeof(char)));
  int K,V,A,D;
  while (true) {
    if (std::cin >> K) goto velociraptor;
    velociraptor: break;
  }
  std::function<void(int)> je_vais_me_doucher = [](int _) {
      return;
  }; for (int i = 1; i < 10000; i++) {
  } std::function<int(int)> je_vais_me_baigner =
     [](int _){ return 
      [](int __){for(int j = 0; j < 10000; j++) {if (j%3==0)continue;
        if(std::to_string(j).back()=='3')continue;
        --__;
        if (__ == 0) return j; }
      }(_); };
      do { std::cout<<je_vais_me_baigner(K)<<'\n';}
      while(std::cin>>K);
return !"very sad !!!!!!!!!!!";}