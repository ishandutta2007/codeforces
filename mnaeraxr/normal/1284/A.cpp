/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author marX
 */

#include <iostream>
#include <fstream>

#include <bits/stdc++.h>

using namespace std;

class ANewYearAndNaming {
public:
    void solve(std::istream &in, std::ostream &out) {
        int n, m;
        in >> n >> m;

        vector<string> a(n), b(m);

        for (int i = 0; i < n; ++i) {
            in >> a[i];
        }

        for (int j = 0; j < m; ++j) {
            in >> b[j];
        }

        int t;
        in >> t;

        while (t--) {
            int q;
            in >> q;
            q--;
            out << a[q % n] << b[q % m] << '\n';
        }
    }
};


int main() {
    ANewYearAndNaming solver;
    std::istream &in(std::cin);
    std::ostream &out(std::cout);
    solver.solve(in, out);
    return 0;
}