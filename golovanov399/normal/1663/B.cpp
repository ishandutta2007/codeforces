#include <bits/stdc++.h>

#define all(x) (x).begin(), (x).end()
#define make_unique(x) sort(all(x)); x.resize(unique(all(x)) - x.begin())

using namespace std;

inline int nxt() {
	int x;
	scanf("%d", &x);
	return x;
}

#define check(y) if (x < y) { cout << y << "\n"; return; }

void solve() {
	int x = nxt();
	check(1200)
	check(1400)
	check(1600)
	check(1900)
	check(2100)
	check(2300)
	check(2400)
	check(2600)
	check(3000)
}

int main() {
	int t = 1; // nxt();
	while (t--) {
		solve();
	}

	return 0;
}