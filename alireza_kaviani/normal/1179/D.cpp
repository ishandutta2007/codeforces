/* Generated by powerful Codeforces Tool
 * You can download the binary file in here https://github.com/xalanq/cf-tool (Windows, macOS, Linux)
 * Author: alireza_kaviani
 * Time: 2020-12-07 12:08:12
**/
#include <bits/stdc++.h>
#include <ext/pb_ds/tree_policy.hpp>
#include <ext/pb_ds/assoc_container.hpp>

using namespace std;
using namespace __gnu_pbds;

template <class T> using Tree = tree<T, null_type, less<T>, rb_tree_tag,tree_order_statistics_node_update>;

typedef long long ll;
typedef long double ld;
typedef pair<ll, ll> pll;
typedef pair<int, int> pii;

#define all(x)                      (x).begin(),(x).end()
#define Sort(x)                     sort(all((x)))
#define X                           first
#define Y                           second
#define sep                         ' '
#define endl                        '\n'
#define SZ(x)                       ll(x.size())

ll poww(ll a, ll b, ll md) {
    return (!b ? 1 : (b & 1 ? a * poww(a * a % md, b / 2, md) % md : poww(a * a % md, b / 2, md) % md));
}

const ll MAXN = 1e6 + 10;
const ll LOG = 22;
const ll INF = 8e18;
const ll MOD = 1e9 + 7; // 998244353; // 1e9 + 9;

bool Q = 1;
struct Line {
	mutable ll k, m, p;
	bool operator<(const Line& o) const { return Q ? k < o.k : p < o.p; }
};

struct CHT : multiset<Line> {
	// (for doubles, use inf = 1/.0, div(a,b) = a/b)
	static const ll inf = LLONG_MAX;
	ll div(ll a, ll b) { // floored division
		return a / b - ((a ^ b) < 0 && a % b); }
	bool isect(iterator x, iterator y) {
		if (y == end()) return x->p = inf, 0;
		if (x->k == y->k) x->p = x->m > y->m ? inf : -inf;
		else x->p = div(y->m - x->m, x->k - y->k);
		return x->p >= y->p;
	}
	void add(ll k, ll m) {
		auto z = insert({k, m, 0}), y = z++, x = y;
		while (isect(y, z)) z = erase(z);
		if (x != begin() && isect(--x, y)) isect(x, y = erase(y));
		while ((y = x) != begin() && (--x)->p >= y->p)
			isect(x, erase(y));
	}
	ll query(ll x) {
		assert(!empty());
		Q = 0 ; auto l = *lower_bound({0 , 0 , x}); Q = 1;
		return l.k * x + l.m;
	}
};

ll n , ans = INF , dp[MAXN] , sz[MAXN];
vector<int> adj[MAXN];

void DFS(int v , int p = -1){
	sz[v] = 1;
	for(int u : adj[v]){
		if(u == p)	continue;
		DFS(u , v);
		sz[v] += sz[u];
	}
	dp[v] = sz[v] * sz[v];
	for(int u : adj[v]){
		if(u == p)	continue;
		dp[v] = min(dp[v] , dp[u] + (sz[v] - sz[u]) * (sz[v] - sz[u]));
	}
	CHT cht;
	for(int u : adj[v]){
		if(u == p)	continue;
		ll cur = dp[u] + sz[u] * sz[u] - 2 * n * sz[u];
		// cout << cur << endl;
		if(SZ(cht)){
			ans = min(ans , -cht.query(sz[u] * 2) + cur + n * n);
		}
		cht.add(-sz[u] , -cur);
	}
	// cout << v << sep << dp[v] << endl;
}

int main() {
	ios::sync_with_stdio(false);cin.tie(0);cout.tie(0);

	cin >> n;
	for(int i = 1 ; i < n ; i++){
		int v , u;
		cin >> v >> u;
		adj[v].push_back(u);
		adj[u].push_back(v);
	}
	DFS(1);
	// cout << ans << endl;
	ans = min(ans , dp[1]);
	cout << (n * n * 2 - ans - n) / 2 << endl;

    return 0;
}
/*

*/