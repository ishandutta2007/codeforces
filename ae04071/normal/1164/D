#include <bits/stdc++.h>
using namespace std;

int n,m;
int main() {
    scanf("%d%d",&n,&m);
    if(m<=n/2) {
        printf("%d\n",max(1,m));
    } else {
        printf("%d\n",n-m);
    }
    return 0;
}