#include <bits/stdc++.h>
using namespace std;

int main(){
	ios::sync_with_stdio(false);
	cin.tie(nullptr);

	int n, m;
	cin >> n >> m;
	for(int i = 0; i < m; i++){
		int l, r;
		cin >> l >> r;
	}
	for(int i = 0; i < n; i++){
		cout << (i&1);
	}
	cout << '\n';

	return 0;
}