#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
vector<int> v;
int main() {
    int t;
    ios::sync_with_stdio(false);
    cin >> t;
    while (t--) {
        int n;
        cin >> n;
        cout << (n / 2) << endl;
    }
}