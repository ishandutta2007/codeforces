/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author
 */

#include <bits/stdc++.h>
#include <ext/pb_ds/tree_policy.hpp>
#include <ext/pb_ds/assoc_container.hpp>

using namespace std;
using namespace __gnu_pbds;

template <class T> using Tree = tree<T, null_type, less<T>, rb_tree_tag,tree_order_statistics_node_update>;

typedef long long ll;
typedef long double ld;
typedef pair<int, int> pii;
typedef pair<ll, ll> pll;
typedef pair<double, double> pdd;
typedef pair<ld, ld> pld;
typedef pair<string, string> pss;

#define all(x)                      (x).begin(),(x).end()
#define Sort(x)                     sort(all((x)))
#define X                           first
#define Y                           second
#define Mp                          make_pair
#define tab                         '\t'
#define sep                         ' '
#define endl                        '\n'
#define debug(x)                    cerr << #x << " = " << x << endl
#define SZ(x)                       ll(x.size())
#define fast_io                     ios::sync_with_stdio(false);cin.tie(0);cout.tie(0);
#define file_io                     freopen("in.txt" , "r+" , stdin) ; freopen("out.txt" , "w+" , stdout);
#define set_random                  mt19937_64 rng(chrono::steady_clock::now().time_since_epoch().count());

inline ll input() {
    ll n;
    cin >> n;
    return n;
}

ll poww(ll a, ll b, ll md) {
    return (!b ? 1 : (b & 1 ? a * poww(a * a % md, b / 2, md) % md : poww(a * a % md, b / 2, md) % md));
}

ll Sqrt(ll x, ll y) {
    ll t = pow(x, 1. / y);
    for (ll i = max(0LL, t - 10); i <= t + 10; i++) {
        if (poww(i, y, ll(8e18)) == x) return i;
    }
    return -1;
}

set_random;

const ll MAXN = 5010;
const ll INF = 1e18;
const ll MOD1 = 998244353;
const ll MOD2 = 1e9 + 7;
const ll MOD3 = 1e9 + 9;

ll n , p[MAXN] , dp[MAXN][MAXN][2];

ll get(ll x , ll y , ll z)
{
    return max(0LL , x - (y - 1)) + max(0LL , z - (y - 1));
}

int main() {
    fast_io;

    for(ll i = 0 ; i < MAXN ; i++)
    {
        for(ll j = 0 ; j < MAXN ; j++)
        {
            dp[i][j][0] = dp[i][j][1] = INF;
        }
    }

    cin >> n;
    for(ll i = 1 ; i <= n ; i++)    cin >> p[i];

    dp[0][0][0] = 0;
    dp[1][0][0] = 0;
    for(ll i = 2 ; i <= n + 1 ; i++)
    {
        for(ll j = 0 ; j <= n ; j++)
        {
            dp[i][j][0] = min(dp[i][j][0] , min(dp[i - 1][j][0] , dp[i - 1][j][1]));
            dp[i][j + 1][1] = min(dp[i][j + 1][1] , dp[i - 2][j][0] + get(p[i - 2] , p[i - 1] , p[i]));
            dp[i][j + 1][1] = min(dp[i][j + 1][1] , dp[i - 2][j][1] + get(min(p[i - 2] , p[i - 3] - 1) , p[i - 1] , p[i]));
        }
    }

    for(ll i = 1 ; i <= (n + 1) / 2 ; i++)
    {
        cout << min(dp[n + 1][i][0] , dp[n + 1][i][1]) << sep;
    }

    return 0;
}
/*
todo :
    1- set constants
    2- check TimeLimit and MemoryLimit
    3- check special test cases
    4- don't forget BS can help you that is good idea(use for loop for floats)
    5- don't forget printf and scanf can help you in your code speed
*/