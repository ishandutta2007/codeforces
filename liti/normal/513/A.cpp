/* In the name of Allah */
#include <bits/stdc++.h>

using namespace std;

#define sz(x) (((int) x.size()))
typedef long long ll;

int main() {
	int n1,n2,k1,k2;
	cin >> n1 >> n2 >> k1 >> k2;
	cout << (n1 > n2?"First" : "Second") << endl;
}