#include <bits/stdc++.h>
using namespace std;

#define rep(i, N) for (int i = 0; i < N; i++)
#define pb push_back

typedef long long ll;

int main() {
	int N; cin >> N;
	cout << (N % 2 == 0 ? "Mahmoud" : "Ehab") << endl;
}