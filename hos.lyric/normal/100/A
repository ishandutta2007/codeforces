int main() {
    string s = Stdio.stdin.gets();
    int n, k, m;
    sscanf(s, "%d%d%d", n, k, m);
    if (n == m && k >= 1 || k >= 4) {
        write("YES\n");
    } else {
        write("NO\n");
    }
    return 0;
}