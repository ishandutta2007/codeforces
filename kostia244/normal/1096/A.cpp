#include <bits/stdc++.h>
using namespace std;

#define MAX 100

long long a, b, n;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	cin >> n;
	while(n--) {
		cin >> a >> b;

		cout << a << " " << (2 * a) << "\n";
	}
}