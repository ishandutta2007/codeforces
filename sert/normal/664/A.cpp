#include <bits/stdc++.h>
using namespace std;
const int N = 1e6 + 34;
typedef double ld;
typedef long long ll;

int main() {
    //freopen("a.in", "r", stdin);
    //freopen("a.out", "w", stdout);

    string a, b;
    cin >> a >> b;
    cout << (a == b ? a : "1");

    return 0;
}