/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author marX
 */

#include <iostream>
#include <fstream>

#include <bits/stdc++.h>

using namespace std;

class AShuffleHashing {
public:
    void solve(std::istream &in, std::ostream &out) {
        int t;
        in >> t;

        while (t--) {
            string a, b;
            in >> a >> b;

            sort(a.begin(), a.end());

            bool ok = false;

            for (int i = 0; i < (int) b.length() && !ok; ++i) {
                auto t = b.substr(i, a.length());
                sort(t.begin(), t.end());

                if (a == t) {
                    ok = true;
                }
            }

            out << (ok ? "YES" : "NO") << endl;
        }
    }
};


int main() {
    AShuffleHashing solver;
    std::istream &in(std::cin);
    std::ostream &out(std::cout);
    solver.solve(in, out);
    return 0;
}