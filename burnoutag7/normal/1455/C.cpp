#include<bits/stdc++.h>
using namespace std;

void mian(){
    int x,y;
    cin>>x>>y;
    cout<<x-1<<' '<<y<<'\n';
}

int main(){

    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int T;
    cin>>T;
    while(T--)mian();

    return 0;
}