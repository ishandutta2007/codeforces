#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
const ll INF = (ll)1e18 + 41;
const int N = (int)1e5;

void solve() {
    ll a, b, c;
    cin >> a >> b >> c;
    cout << (a + b + c) / 2 << "\n";
}

int main() {
    //freopen("a.in", "r", stdin);
    int t;
    cin >> t;
    while (t--) solve();
    return 0;   
}