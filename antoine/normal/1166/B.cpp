/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author
 */

#include <iostream>
#include <fstream>

#include <bits/stdc++.h>

using namespace std;
typedef long long ll;
#define DBG(v) cerr << #v << " = " << (v) << endl;


class BAllTheVowelsPlease {
public:


    void f(int n, const int m, std::ostream& cout) {
        string s = "aeiou";

        while (n--) {
            cout << s;
            cout << string(m - 5, s.back());
            rotate(s.begin(), s.begin() + 1, s.end());
        }
    }

	void solve(std::istream& cin, std::ostream& cout) {
        int n;
        cin >> n;
        for (int i = 5; i < n; ++i) {
            if (n % i == 0 && n / i >= 5) {

                f(i, n / i, cout);
return;
            }
        }
        cout << -1;
	}
};


int main() {
	BAllTheVowelsPlease solver;
	std::istream& in(std::cin);
	std::ostream& out(std::cout);
	solver.solve(in, out);
	return 0;
}