#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using ld = long double;

signed main() {
  cout << "Odd" << '\n';
  return 0;
  int x;
  assert(cin >> x);
  string tmp;
  assert(!(cin >> tmp));
  if (x == 1) {
    cout << 6 << '\n';
    return 0;
  }
  cout << 6 << '\n';
}