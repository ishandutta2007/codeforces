#include <iostream>
#include <vector> // vector
#include <algorithm> // sort
#include <math.h> // math
#include <map> // map
#include <string> // string
using namespace std;
#define lli long long int
vector <lli> nums;
int main () {
	int t;
	cin >> t;
	for (int a=0;a<t;++a) {
		lli n,x;
		cin >> n >> x;
		cout <<  2*x << endl;
	}
}