int main () {
    int n = (int)Stdio.stdin->gets();
    string s = Stdio.stdin->gets();
    string t = Stdio.stdin->gets();
    int a = strlen(s);
    int b = strlen(t);
    int x = 0;
    while(x<a && x<b && s[x] == t[x])x++;
    if((a-x)+(b-x) <= n) write("Yes\n"); else write("No\n");
}