/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author
 */

// Actual solution is at the bottom

#undef NDEBUG

#include <algorithm>
#include <array>
#include <bitset>
#include <cassert>
#include <climits>
#include <cstdint>
#include <cmath>
#include <complex>
#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <functional>
#include <iomanip>
#include <iostream>
#include <map>
#include <memory>
#include <numeric>
#include <queue>
#include <random>
#include <set>
#include <stack>
#include <string>
#include <unordered_set>
#include <unordered_map>
#include <vector>
// AtCoder library from https://github.com/atcoder/ac-library
// #include "../atcoder/all"

#define sz(v) ((int)(v).size())
#define all(v) (v).begin(),(v).end()

using namespace std;

typedef int64_t int64;
typedef pair<int, int> ii;

class ASdelaiYegoVozrastayushchim {
 public:
  void solveOne() {
    int n;
    cin >> n;
    vector<int64> a(n);
    for (auto& x : a) cin >> x;
    int64 res = numeric_limits<int64>::max();
    for (int mid = 0; mid < n; ++mid) {
      int64 cur = 0;
      int64 sofar = 0;
      for (int i = mid + 1; i < n; ++i) {
        sofar = (sofar / a[i] + 1);
        cur += sofar;
        sofar *= a[i];
      }
      sofar = 0;
      for (int i = mid - 1; i >= 0; --i) {
        sofar = (sofar / a[i] + 1);
        cur += sofar;
        sofar *= a[i];
      }
      res = min(res, cur);
    }
    cout << res << "\n";
  }

  void solve() {
    int nt = 1;
    for (int it = 0; it < nt; ++it) {
      // cout << "Case #" << (it + 1) << ": ";
      solveOne();
    }
  }
};

// #define st_mtimespec st_mtim

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(0);
    ASdelaiYegoVozrastayushchim solver;


    solver.solve();
    return 0;
}