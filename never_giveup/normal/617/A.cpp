#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main(){
    //ifstream cin("queries.in");
    //ofstream cout("queries.out");
    ios::sync_with_stdio(0);
    int x;
    cin >> x;
    x += 4;
    cout << x / 5;
}