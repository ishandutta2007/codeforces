#include <iostream>

#define int long long

signed main() {
  std::ios::sync_with_stdio(false);
  std::cin.tie(0);

  int t;
  std::cin >> t;

  while(t--) {
    int n, m, i, j;
    std::cin >> n >> m >> i >> j;

    std::cout << 1 << " " << 1 << " " << n << " " << m << std::endl;
  }

  return 0;
}