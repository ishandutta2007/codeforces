/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author marX
 */

#include <iostream>
#include <fstream>

#include <bits/stdc++.h>

using namespace std;

class ASashaAndHisTrip {
public:
    void solve(std::istream& in, std::ostream& out) {
        int n, v; in >> n >> v;

        int ans = 0;

        int x = n - 1;

        for (int i = 1; i <= n && x; ++i) {
            if (i == 1) {
                int c = min(v, n - 1);
                ans += c;
                x -= c;
            } else {
                x--;
                ans += i;
            }
        }

        out << ans << endl;
    }
};


int main() {
	ASashaAndHisTrip solver;
	std::istream& in(std::cin);
	std::ostream& out(std::cout);
	solver.solve(in, out);
	return 0;
}