/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author
 */

// Actual solution is at the bottom

#undef NDEBUG

#include <algorithm>
#include <array>
#include <bitset>
#include <cassert>
#include <climits>
#include <cstdint>
#include <cmath>
#include <complex>
#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <functional>
#include <iomanip>
#include <iostream>
#include <map>
#include <memory>
#include <numeric>
#include <queue>
#include <random>
#include <set>
#include <stack>
#include <string>
#include <unordered_set>
#include <unordered_map>
#include <vector>
// AtCoder library from https://github.com/atcoder/ac-library
// #include "../atcoder/all"

#define sz(v) ((int)(v).size())
#define all(v) (v).begin(),(v).end()

using namespace std;

typedef int64_t int64;
typedef pair<int, int> ii;

class DTsiklicheskiiSdvig {
 public:
  void solveOne() {
    int n;
    cin >> n;
    vector<int> a(n), b(n);
    for (auto& x : a) cin >> x;
    for (auto& x : b) cin >> x;
    reverse(all(a));
    reverse(all(b));
    a.push_back(0);
    b.push_back(0);
    multiset<int> reserve;
    int j = 0;
    for (int i = 0; i < a.size(); ++i) {
      if (i == 0 || b[i] != b[i - 1]) {
        while (a[j] != b[i]) {
          auto it = reserve.find(a[j]);
          if (it == reserve.end()) {
            cout << "NO\n";
            return;
          }
          reserve.erase(it);
          ++j;
        }
        ++j;
      } else {
        reserve.insert(b[i]);
      }
    }
    assert(j == a.size());
    cout << "YES\n";
  }

  void solve() {
    int nt;
    cin >> nt;
    for (int it = 0; it < nt; ++it) {
      // cout << "Case #" << (it + 1) << ": ";
      solveOne();
    }
  }
};

// #define st_mtimespec st_mtim

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(0);
    DTsiklicheskiiSdvig solver;


    solver.solve();
    return 0;
}