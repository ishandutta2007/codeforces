#include <bits/stdc++.h>
using namespace std;

int t;
int n, x;

int main()
{
    scanf("%d", &t);
    while (t--) {
        scanf("%d %d", &n, &x);
        printf("%d\n", 2 * x);
    }
    return 0;
}