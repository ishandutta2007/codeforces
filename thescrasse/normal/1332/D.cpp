#include <bits/stdc++.h>
using namespace std;

long long i, i1, j, k, k1, t, n, m, res, check1, a, b;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    //ifstream cin("input.txt");
    //ofstream cout("output.txt");

    cin >> k;
    cout << 2 << ' ' << 3 << endl;
    cout << 262143 << ' ' << 131071 << ' ' << 131071 - k << endl;
    cout << 131072 << ' ' << 262143 << ' ' << 131071 << endl;

    return 0;
}