#include <bits/stdc++.h>

using namespace std;

int n, m;

int main(void) {
    ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    cin >> n >> m;
    for (int i = 0; i < 2000; ++i) cout << 1; cout << endl;
    for (int i = 0; i + 1 < 2000; ++i) cout << 8; cout << 9; cout << endl;
    return 0;
}