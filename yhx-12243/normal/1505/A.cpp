#include <bits/stdc++.h>
using std::cin;
using std::cout;

int main() {
	int i;
	std::string s;
	std::ios::sync_with_stdio(false), cin.tie(nullptr);
	for (; std::getline(cin, s); ) {
		cout << "NO" << std::endl;
	}
	return 0;
}