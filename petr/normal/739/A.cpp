/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author Petr
 */

#include <iostream>
#include <fstream>

#include <iostream>
#include <vector>

using namespace std;

typedef long long ll;

class TaskA {
public:
	void solve(istream& in, ostream& out) {
        int n, m;
        in >> n >> m;
        vector<pair<int, int>> seg(m);
        for (auto& s : seg) {
            in >> s.first >> s.second;
        }
        int res = n;
        for (const auto& s : seg) {
            res = min(res, s.second - s.first + 1);
        }
        out << res << "\n";
        for (int i = 0; i < n; ++i) {
            if (i > 0) out << " ";
            out << i % res;
        }
        out << "\n";
	}
};

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	TaskA solver;
	std::istream& in(std::cin);
	std::ostream& out(std::cout);
	solver.solve(in, out);
	return 0;
}