#include <iostream>
#include <cstdio>
using namespace std;
#define rep(i,a,b) for(int i = (a); i < int(b); i++)

int main(){
	cin.sync_with_stdio(0);
	string ans = "23121";
	int i;
	cin >> i;
	cout << ans[i-1] << endl;
}