#include <bits/stdc++.h>

using namespace std;

int main() {
#ifdef wxh010910
  freopen("input.txt", "r", stdin);
#endif
  int n;
  string s;
  cin >> n >> s;
  sort(s.begin(), s.end());
  cout << s << endl;
  return 0;
}