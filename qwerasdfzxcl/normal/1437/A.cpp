#include <bits/stdc++.h>

using namespace std;

int main()
{
    int t;
    scanf("%d", &t);
    for (int z=0;z<t;z++){
        int n, m, ans;
        scanf("%d %d", &n, &m);
        if (m<n*2) printf("YES\n");
        else printf("NO\n");
    }
    return 0;
}