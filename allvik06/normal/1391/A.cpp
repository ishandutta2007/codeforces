#include <bits/stdc++.h>

using namespace std;
#define ll long long
inline int min(const int &a, const int &b) {
    if (a < b) return a;
    else return b;
}

inline int solve1() {

}

inline int solve2() {

}

signed main() {
    srand(time(0));
    if (1) {
        ios_base::sync_with_stdio(false);
        cin.tie(nullptr);
        cout.tie(nullptr);
    }
    if (1) {
        int t;
        cin >> t;
        while (t--) {
            int n;
            cin >> n;
            for (int i = 1; i <= n; ++i) cout << i << " ";
            cout << "\n";
        }
    }
    else {

    }
}