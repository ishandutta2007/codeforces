#include <bits/stdc++.h>
using namespace std;

const int maxn = 1e5 + 10;

int main() {
    ios_base::sync_with_stdio(false); cin.tie(0);
    string a; cin >> a;
    cout << a;
    reverse(a.begin(), a.end());
    cout << a << endl;
    return 0;
}