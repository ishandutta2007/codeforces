#include <bits/stdc++.h>
using namespace std;
 
string s;

int solve() {
  while (getline(cin, s)) {
    cout << "NO" << endl;
  }
  return 0;
}

int main() {
  int t = 1;
  // scanf("%d", &t);
  for (int tc = 0; tc < t; ++tc) {
    // printf("Case #%d: ", tc+1);
    solve();
  }
  return 0;
}