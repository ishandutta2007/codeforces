#include <bits/stdc++.h>

using int64 = long long;

const int N = 1000005;

int T, n, x, y, z, ans;
int a[N];

int main() {
    std::ios::sync_with_stdio(0);
    std::cin.tie(0), std::cout.tie(0);
    std::cin >> T;
    while (T--) {
        std::cin >> n;
        std::cout << 2 << ' ' << n - 1 << '\n';
    }
    return 0;
}