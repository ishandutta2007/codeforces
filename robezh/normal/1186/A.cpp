#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
const int N = (int)1e5 + 50;

int a, b, c;

int main() {
    cin >> a >> b >> c;
    cout << (b >= a && c >= a ? "YES" : "NO") << endl;
}