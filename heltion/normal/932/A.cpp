#include<bits/stdc++.h>
using namespace std;
typedef long long LL;
auto no = [](){cout << "No";exit(0);}; 
#define maxn
int main(){
	ios::sync_with_stdio(false);
	string A;
	cin >> A;
	cout << A;
	reverse(A.begin(), A.end());
	cout << A;
}