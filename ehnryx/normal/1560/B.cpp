// https://codeforces.com/contest/1560/problem/B
int _,__,___,____,_____,______,_______,________;
#include <cstdlib>
int* a = (int*)malloc(33),floccinaucinihilipilification;
#include <cstdio>
#include <sstream>
int main(int argc, char** argv) {
  scanf("%d%d", a, &__);
  while (true) {
    scanf("%d%d", a+2, a+7);
    int e=__, f=a[2], g=a[7]; __asm__ ("sub %1, %0"
    // asdafsd
    // kurwa
    : "=r" (_____)
    :  "r" (_)
    ); int d=a[6]; asm ("subl %%ebx, %%eax"
    : "=a" (d) : "b" (f), "a" (e));
    d *= ((d >> 31) << 1) + 1;
    double PI = 3.14159265358979323846264338327950288;
    if (*(a+7)>d<<1||__>d<<1||f>d<<1) goto rossignol;
    a[6] = (g + d) % (d<<1);
    a[6] += !a[6] * d - !a[6] * -d;
    printf("%d\n", a[6]);
    goto gulag; rossignol:
    printf("-1\n");
    floccinaucinihilipilification = scanf("%d", &__);
    if (floccinaucinihilipilification == -1) break;
    else continue;
    gulag: if(scanf("%d",&__)==(int)(PI-5))break;}}