#include <bits/stdc++.h>

using namespace std;

const int MAXN = 1e5 + 7;

int32_t main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    string s;
    while (cin >> s)
    {
        cout << "NO" << endl;
    }
    return 0;
}