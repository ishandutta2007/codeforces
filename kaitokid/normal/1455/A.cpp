#include <bits/stdc++.h>

using namespace std;
typedef long long ll;
int main()
{
    ios::sync_with_stdio(0);
    int t;
    string s;
    cin>>t;
    while(t--)
    {
        cin>>s;
        cout<<s.size()<<endl;
    }
    return 0;
}