
#include <bits/stdc++.h>

#define ll long long
using namespace std;

int t, a, b, c, d;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cin >> t;
    while(t--) {
        cin >> a >> b >> c >> d;
        cout << b << " " << c << " " << c << '\n';
    }
}