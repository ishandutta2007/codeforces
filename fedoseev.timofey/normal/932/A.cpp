#include <iostream>
#include <vector>
#include <algorithm>
#include <set>
#include <map>
#include <cmath>
#include <queue>

using namespace std;

typedef long long ll;

int main() {
    string a;
    cin >> a;
    cout << a;
    reverse(a.begin(), a.end());
    cout << a;
}