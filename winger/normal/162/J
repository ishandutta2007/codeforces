USING: io kernel math math.parser sequences locals ;
readln { 0  t } [| pair c | c CHAR: ( = 1 -1 ? pair first + dup 0 >= pair second and { } 2sequence ] reduce
dup first 0 = swap second and "YES" "NO" ? print