#include <map>
#include <set>
#include <stack>
#include <cmath>
#include <queue>
#include <vector>
#include <cstdio>
#include <string>
#include <cstring>
#include <cstdlib>
#include <iostream>
#include <algorithm>

using namespace std;

int main() {
    //freopen("input.txt", "r", stdin);
    //freopen("output.txt", "w", stdout);
    cout << 2000 << endl;
    for (int i = 1; i <= 1000; ++i) {
        cout << i << " " << 1 << " " << i << " " << 2 << endl;
    }
    for (int i = 1000; i >= 1; --i) {
        cout << i << " " << 1 << " " << i << " " << 2 << endl;
    }
    return 0;
}