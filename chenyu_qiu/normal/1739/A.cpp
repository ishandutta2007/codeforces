#include<bits/stdc++.h>
using namespace std;
int main(){
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	cout.tie(nullptr);
	int TT;
	cin>>TT;
	while(TT--){
		int n,m;
		cin>>n>>m;
		cout<<(n+1)/2<<" "<<(m+1)/2<<"\n";
	}
	return 0;
}