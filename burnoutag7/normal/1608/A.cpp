#include<bits/stdc++.h>
using namespace std;

void mian(){
    int n;
    cin>>n;
    for(int i=1;i<=n;i++)cout<<i+1<<' ';
    cout<<'\n';
}

int main(){

    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int T;
    cin>>T;
    while(T--)mian();

    return 0;
}