#include <iostream>
#include <stdio.h>
using namespace std;

int t;
int n, m, x, y;

int main()
{
    int test;
    int i, j;

    scanf("%d", &t);

    for (test=1;test<=t;test++)
    {
        scanf("%d %d %d %d", &n, &m, &x, &y);

        printf("%d %d %d %d\n", 1, 1, n, m);
    }
}