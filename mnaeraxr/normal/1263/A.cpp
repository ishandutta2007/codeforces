/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author marX
 */

#include <iostream>
#include <fstream>

#include <bits/stdc++.h>

using namespace std;

class ASweetProblem {
public:
    void solve(std::istream &in, std::ostream &out) {
        int t;
        in >> t;

        while (t-- > 0) {
            int a, b, c;
            in >> a >> b >> c;
            if (a > b) swap(a, b);
            if (b > c) swap(b, c);

            int ans = min((a + b + c) / 2, a + b);

            out << ans << endl;
        }
    }
};


int main() {
    ASweetProblem solver;
    std::istream &in(std::cin);
    std::ostream &out(std::cout);
    solver.solve(in, out);
    return 0;
}