#include <bits/stdc++.h>

using namespace std;
#define ll long long

ll solve1(ll n) {
    return n / 2 + 1;
}

template <typename T> T solve2() {

}

signed main() {
    srand(time(nullptr));
    if (1) {
        ios_base::sync_with_stdio(false);
        cin.tie(nullptr);
        cout.tie(nullptr);
    }
    if (1) {
        int t;
        cin >> t;
        while (t--) {
            ll n;
            cin >> n;
            cout << solve1(n) << "\n";
        }
    }
    else {
        int t;
        cin >> t;
        while (t--) {

        }
    }
}