#include <bits/stdc++.h>
#define long long long
#define pb push_back
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    long n;
    cin>>n;
    cout<<(n+1)/2;
    return 0;
}