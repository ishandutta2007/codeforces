#include <iostream>
#include <algorithm>
using namespace std;
int T, N, M;
int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cin >> T;
	while (T--)
	{
		cin >> N;
		int i;
		for (i = 0; i < N; i++)
		{
			cout << 1 << ' ';
		}
		cout << '\n';
	}
}