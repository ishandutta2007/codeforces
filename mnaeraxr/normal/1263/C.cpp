/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author marX
 */

#include <iostream>
#include <fstream>

#include <bits/stdc++.h>

using namespace std;

class CEveryoneIsAWinner {
public:
    void solve(std::istream &in, std::ostream &out) {
        int t;
        in >> t;

        while (t-- > 0) {
            int n;
            in >> n;

            set<int> s;

            for (int i = 1; i * i <= n; ++i) {
                s.insert(i);
                s.insert(n / i);
            }

            s.insert(0);

            out << s.size() << endl;

            for (auto x : s) {
                out << x << " ";
            }

            out << endl;
        }

    }
};


int main() {
    CEveryoneIsAWinner solver;
    std::istream &in(std::cin);
    std::ostream &out(std::cout);
    solver.solve(in, out);
    return 0;
}