#include <bits/stdc++.h>

using namespace std;
int w;
string s;
int main()
{
    ios_base::sync_with_stdio(false);
    cin >> w;
    while(w--) {
        cin >> s;
        cout << s.size() << "\n";
    }
    return 0;
}