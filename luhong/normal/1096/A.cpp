#include <cstring>
#include <cstdio>
#include <algorithm>
#include <map>
#include <iostream>
#include <set>
#include <vector>
#include <bitset>

int main()
{
	int T; scanf("%d", &T);
	while(T--)
	{
		int a, b; scanf("%d%d", &a, &b);
		printf("%d %d\n", a, a * 2);
	}
}