#include <bits/stdc++.h>

using namespace std;
#define int long long

int n, m, k;
string s;
vector <int> a;
const int MAXN = 1e6 + 10;
int arr[MAXN];


void read() {
	int a, b, c, d;
	cin >> a >> b >> c >> d;
	if (a == d && (c == 0 || a > 0))
		cout << 1;
	else
		cout << 0;
}

void run() {
	
}

void write() {
	
}

signed main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	read();
	run();
	write();
	return 0;
}