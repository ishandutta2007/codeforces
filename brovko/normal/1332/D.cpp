#include <bits/stdc++.h>
#define int long long

using namespace std;
const int INF=131071;

int k;

int32_t main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    cin >> k;
    cout << 2 << ' ' << 3 << endl;
    cout << 2*INF+1 << ' ' << INF << ' ' << INF+1 << endl << INF+1 << ' ' << 2*INF+1 << ' ' << k;
}