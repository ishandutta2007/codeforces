#include <bits/stdc++.h>


using namespace std;



int main() {
    //fstream cin("A.in");
    //ofstream cout("A.out");
    long long n, k;
    //ios::sync_with_stdio(false);
    cin >> n >> k;
    cout << (n / k) * k + k;
    //cin.close();
    //cout.close();
    return 0;
}