#include<bits/stdc++.h>
#define R for(i=0;i<n;i++)
using namespace std;
int n,i,t,a[25],p[25];
int C(int u,int v){return a[u]<a[v];}
main(){
	cin>>n;R cin>>a[p[i]=i]; 
	sort(p,p+n,C);
	R if(i)swap(a[p[i]],a[p[i-1]]);
	R cout<<a[i]<<' ';
}