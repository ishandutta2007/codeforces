
#include <bits/stdc++.h>
using namespace std;
using ll=long long;
using ull=unsigned long long;
#define rep(i,n) for(int i=0; i<(n); i++)

int main(){
  while(true){
    string Q;
    getline(cin,Q);
    if(cin.eof()) break;
    cout<<"NO"<<endl;
  }
  return 0;
}