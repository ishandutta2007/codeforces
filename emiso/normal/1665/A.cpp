#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

#define MN 1001000

ll n, a, b, c, d;
//char s[MN]; string str;

int main() {
    scanf("%lld", &n);
    for(int i = 0; i < n; i++) {
        scanf("%lld", &a);
        printf("%lld %lld %lld %lld\n", a - 3, 1, 1, 1);
    }
    return 0;
}