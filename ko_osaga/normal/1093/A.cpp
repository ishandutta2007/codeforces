#include <bits/stdc++.h>
using namespace std;
using lint = long long;
using pi = pair<int, int>;
const int MAXN = 200005;

char str[MAXN];

int main(){
	int tc; scanf("%d",&tc);
	while(tc--){
		int n; cin >> n; cout << n / 2 << endl;
	}
}