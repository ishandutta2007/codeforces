program Project2;

{$APPTYPE CONSOLE}

uses
  SysUtils;

//const alph = '23456789TJQKA';
var N, X, y, t : int64;
    i : longint;
//
begin
//assign(input, 'input.txt'); reset(input);
//
readln(N, x, y);
t := y-(n-1);
if ((t*t+(n-1)*1) >= x ) and (t>0) then begin
  for i := 1 to n-1 do Writeln(1);
  Writeln(t);
end else Writeln(-1);
//
//close(input);
end.