#include<bits/stdc++.h>
using namespace std;

char s[105];
int n;

void mian(){
    cin>>s+1;
    n=strlen(s+1);
    cout<<n<<'\n';
}

int main(){

    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int T;
    cin>>T;
    while(T--)mian();

    return 0;
}