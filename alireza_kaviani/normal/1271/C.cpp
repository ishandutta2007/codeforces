/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author alireza_kaviani
 */

#include <bits/stdc++.h>
#include <ext/pb_ds/tree_policy.hpp>
#include <ext/pb_ds/assoc_container.hpp>

using namespace std;
using namespace __gnu_pbds;

template<class T> using Tree = tree<T, null_type, less<T>, rb_tree_tag, tree_order_statistics_node_update>;

typedef long long ll;
typedef long double ld;
typedef pair<ll, ll> pll;
typedef pair<double, double> pdd;

#define all(x)                      (x).begin(),(x).end()
#define Sort(x)                     sort(all((x)))
#define X                           first
#define Y                           second
#define Mp                          make_pair
#define sep                         ' '
#define endl                        '\n'
#define debug(x)                    cerr << #x << " = " << x << endl
#define SZ(x)                       ll(x.size())
#define fast_io                     ios::sync_with_stdio(false);cin.tie(0);cout.tie(0);
#define set_random                  mt19937_64 rng(chrono::steady_clock::now().time_since_epoch().count());

ll poww(ll a, ll b, ll md) {
    return (!b ? 1 : (b & 1 ? a * poww(a * a % md, b / 2, md) % md : poww(a * a % md, b / 2, md) % md));
}

const ll MAXN = 1e6 + 10;
const ll LOG = 22;
const ll INF = 8e18;
const ll MOD = 1e9 + 7; // 998244353; // 1e9 + 9;

ll n, sx, sy, x[MAXN], y[MAXN];

int main() {
    fast_io;

    cin >> n >> sx >> sy;
    for (ll i = 0; i < n; i++) cin >> x[i] >> y[i];

    sort(x, x + n);
    sort(y, y + n);

    ll A = lower_bound(x, x + n, sx) - x, B = n - (upper_bound(x, x + n, sx) - x);
    ll C = lower_bound(y, y + n, sy) - y, D = n - (upper_bound(y, y + n, sy) - y);
    ll mx = max({A, B, C, D});

    if (A == mx) return cout << A << endl << sx - 1 << sep << sy << endl, 0;
    if (B == mx) return cout << B << endl << sx + 1 << sep << sy << endl, 0;
    if (C == mx) return cout << C << endl << sx << sep << sy - 1 << endl, 0;
    if (D == mx) return cout << D << endl << sx << sep << sy + 1 << endl, 0;

    return 0;
}
/*

*/