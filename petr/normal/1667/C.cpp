/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author
 */

// Actual solution is at the bottom

#undef NDEBUG

#include <algorithm>
#include <array>
#include <bitset>
#include <cassert>
#include <climits>
#include <cstdint>
#include <cmath>
#include <complex>
#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <functional>
#include <iomanip>
#include <iostream>
#include <map>
#include <memory>
#include <numeric>
#include <queue>
#include <random>
#include <set>
#include <stack>
#include <string>
#include <unordered_set>
#include <unordered_map>
#include <vector>
// AtCoder library from https://github.com/atcoder/ac-library
// #include "../atcoder/all"

#define sz(v) ((int)(v).size())
#define all(v) (v).begin(),(v).end()

using namespace std;

typedef int64_t int64;
typedef pair<int, int> ii;

class CPokritiePoluferzyami {
 public:
  void solveOne() {
    int n;
    cin >> n;
    int top = 0;
    while (true) {
      ++top;
      int bottom = n - top;
      if (2 * top - 1 > bottom) {
        --top;
        break;
      }
    }
    int bottom = n - top;
    cout << bottom << "\n";
    for (int i = 0; i < top; ++i) {
      int r = i;
      int c = top - 1 - i;
      cout << (r + 1) << " " << (c + 1) << "\n";
    }
    for (int i = 0; i < top - 1; ++i) {
      int r = bottom - 1 - i;
      int c = bottom - 1 - (top - 2 - i);
      cout << (r + 1) << " " << (c + 1) << "\n";
    }
    for (int i = top; i < min(n, bottom - (top - 1)); ++i) {
      cout << (i + 1) << " " << (i + 1) << "\n";
    }
  }

  void solve() {
    int nt = 1;
    for (int it = 0; it < nt; ++it) {
      // cout << "Case #" << (it + 1) << ": ";
      solveOne();
    }
  }
};

// #define st_mtimespec st_mtim

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(0);
    CPokritiePoluferzyami solver;


    solver.solve();
    return 0;
}