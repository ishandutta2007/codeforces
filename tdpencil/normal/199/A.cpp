#include <bits/stdc++.h>
using namespace std;
const int INF = 1e9+7, M = (1e9+7), MAXN = 1 * 2e5;
int A[MAXN], C[MAXN];


void solve() {
    int X; cin >> X;
    cout << 0 << " " << 0 << " " << X << endl;
}
int main() {
    cin.tie(0)->sync_with_stdio(0);
    int T = 1; // cin >> T;

    while(T--) {
        solve();
    }
}