#include <bits/stdc++.h>

using namespace std;
int w, a, b, c, d;
int main()
{
    cin >> w;
    while(w--) {
        cin >> a >> b >> c >> d;
        cout << b << " " << c << " " << c << "\n";
    }
    return 0;
}