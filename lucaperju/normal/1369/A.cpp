#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    int i,j,n,t,m;
    cin>>t;
    while(t--)
    {
        int a;
        cin>>a;
        if(a%4==0)
            cout<<"Yes\n";
        else
            cout<<"No\n";
    }
    return 0;
}