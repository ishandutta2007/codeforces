/* Generated by powerful Codeforces Tool
 * You can download the binary file in here https://github.com/xalanq/cf-tool (Windows, macOS, Linux)
 * Author: alireza_kaviani
 * Time: 2020-04-26 19:05:03
**/
#include <bits/stdc++.h>
#include <ext/pb_ds/tree_policy.hpp>
#include <ext/pb_ds/assoc_container.hpp>

using namespace std;
using namespace __gnu_pbds;

template <class T> using Tree = tree<T, null_type, less<T>, rb_tree_tag,tree_order_statistics_node_update>;

typedef long long ll;
typedef long double ld;
typedef pair<ll, ll> pll;

#define all(x)                      (x).begin(),(x).end()
#define Sort(x)                     sort(all((x)))
#define X                           first
#define Y                           second
#define sep                         ' '
#define endl                        '\n'
#define SZ(x)                       ll(x.size())

ll poww(ll a, ll b, ll md) {
    return (!b ? 1 : (b & 1 ? a * poww(a * a % md, b / 2, md) % md : poww(a * a % md, b / 2, md) % md));
}

const ll MAXN = 2e5 + 10;
const ll LOG = 22;
const ll INF = 8e18;
const ll MOD = 998244353; // 1e9 + 9;

ll n , k , ans , fact[MAXN] , inv[MAXN];

ll C(ll r , ll n){
    if(r > n || r < 0 || n < 0) return 0;
    return fact[n] * inv[r] % MOD * inv[n - r] % MOD;
}

ll f(ll x){
    ll ans = 0;
    for(ll i = 0 ; i < x ; i++) ans += (i % 2 == 0 ? 1ll : -1ll) * C(i , x) * poww(x - i , n , MOD) % MOD;
    return (ans % MOD + MOD) % MOD;
}

int main() {
    ios::sync_with_stdio(false);cin.tie(0);cout.tie(0);

    fact[0] = inv[0] = 1;
    for(ll i = 1 ; i < MAXN ; i++)  fact[i] = fact[i - 1] * i % MOD , inv[i] = poww(fact[i] , MOD - 2 , MOD);
    cin >> n >> k;
    if(k >= n)  return cout << 0 << endl , 0;
    if(k == 0)  return cout << fact[n] << endl , 0;
    cout << 2ll * C(k , n) * f(n - k) % MOD << endl;

    return 0;
}
/*

*/