#include <bits/stdc++.h>
int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
    int t;
    std::cin >> t;
    while (t--) {
        int n, m, i, j;
        std::cin >> n >> m >> i >> j;
        std::cout << 1 << " " << 1 << " " << n << " " << m << "\n";
    }
    return 0;
}