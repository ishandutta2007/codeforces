/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author RiaD
 */

#include <iostream>
#include <fstream>

#include <iostream>

class A {
public:
	void solve(std::istream& in, std::ostream& out) {
		//(kt)b + t;
		//(kb + 1)t;

		long long a, b, ans = 0;
		const int mod = 1000000007;
		in >> a >> b;
		for (int t = 1; t < b; ++t) {
			long long res = ((b + 1) * t + (a * b % mod + 1)* t)  % mod;
			res *= a;
			res %= mod;
			res *= (mod + 1) / 2;
			res %= mod;
			ans += res;
		}
		out << ans % mod;
	}
};


int main() {
	A solver;
	std::istream& in(std::cin);
	std::ostream& out(std::cout);
	solver.solve(in, out);
	return 0;
}