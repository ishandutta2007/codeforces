#include <bits/stdc++.h>
using namespace std;

void solve(){
    int n, m, i, j;
    cin >> n >> m >> i >> j;
    cout << 1 << " " << 1 << " " << n  << " " << m << "\n";
}

int main(){
    // freopen("a.in", "r", stdin);
    cin.tie(0)->sync_with_stdio(0);
    int t; cin >> t;
    while(t--) solve();
}