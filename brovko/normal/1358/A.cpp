#include <bits/stdc++.h>
#define int long long
#define y1 uwiegewiug

using namespace std;

int t, n, m;

int32_t main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    cin >> t;
    while(t--)
    {
        cin >> n >> m;
        cout << (n*m+1)/2 << "\n";
    }
}