#include <bits/stdc++.h>
using namespace std;
#define int long long

main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	int t, x;
	cin >> t;
	while (t--) {
		cin >> x;
		cout << "1 " << x - 1 << '\n';
	}
}