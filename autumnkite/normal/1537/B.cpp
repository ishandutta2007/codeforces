#include <bits/stdc++.h>

void solve() {
	int n, m, x, y;
	std::cin >> n >> m >> x >> y;
	std::cout << 1 << " " << 1 << " " << n << " " << m << "\n";
}

int main() {
	std::ios_base::sync_with_stdio(false);
	std::cin.tie(0);

	int T;
	std::cin >> T;
	while (T--) {
		solve();
	}
}