#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef long double ld;

const int N = 1e5;
const ld EPS = 1e-8;

int a[N];

int main()
{
//    freopen("a.in", "r", stdin);
    //freopen("a.out", "w", stdout);
    int a, b, c, d;
    cin >> a >> b >> c >> d;
    if (a > b)
        cout << "First";
    else
        cout << "Second";
}