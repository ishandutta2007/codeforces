var n,i,j,k,l,r:longint;
  begin
    read(n);
    for i:=1 to n do
     begin
       read(j);l:=l+j;
       read(j);r:=r+J;
       READ(J);K:=K+J;
     end;
    if (k=0) and (r=0) and (l=0) then writeln('YES') else writeln('NO');
  end.