#include <bits/stdc++.h>
#define rep(i, a, b) for (int i = a; i <= b; i++)
#define per(i, a, b) for (int i = b; a <= i; i--)
using namespace std;

int T, n, m, i, j;

int main() {
	cin >> T;
	while (T--) {
		cin >> n >> m >> i >> j;
		cout << 1 << " " << 1 << " " << n << " " << m << endl;
	}
	return 0;
}