#include <bits/stdc++.h>
 
using namespace std;
 
typedef long long ll;
typedef long double ld;

int main() {
	string s;
	cin >> s;
	reverse(s.begin(), s.end());
	cout << s;
	reverse(s.begin(), s.end());
	cout << s;
	//system("pause");
    return 0;
}