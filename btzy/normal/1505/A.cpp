#include <bits/stdc++.h>
using namespace std;
int main(){
    ios_base::sync_with_stdio(false);
    string str;
    while(true){
        getline(cin,str);
        if(str.empty())break;
        cout<<"NO"<<endl;
    }
}