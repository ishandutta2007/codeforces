#include<bits/stdc++.h>
using namespace std;

void mian(){
    int n,m,i,j;
    cin>>n>>m>>i>>j;
    cout<<"1 1 "<<n<<' '<<m<<'\n';
}

int main(){

    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int T;
    cin>>T;
    while(T--)mian();

    return 0;
}