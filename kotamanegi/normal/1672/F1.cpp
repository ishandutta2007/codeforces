using namespace std;
#include "bits/stdc++.h"

typedef string::const_iterator State;
#define eps 1e-8L
#define MAX_MOD 1000000007LL
#define GYAKU 500000004LL
#define MOD 998244353LL
#define pb push_back
#define mp make_pair
typedef long long ll;
typedef long double ld;
#define REP(a, b) for (long long(a) = 0; (a) < (b); ++(a))
#define ALL(x) (x).begin(), (x).end()

#define int long long

void solve()
{
    int n;
    cin >> n;
    vector<int> inputs;
    REP(i, n)
    {
        int a;
        cin >> a;
        inputs.push_back(a);
    }
    map<int, vector<int>> gogo;
    REP(i, n)
    {
        gogo[inputs[i]].push_back(i);
    }
    vector<vector<int>> nexts;
    for (auto x : gogo)
    {
        nexts.push_back(x.second);
    }
    vector<vector<int>> next_moves;
    sort(ALL(nexts), [](auto l, auto r)
         { return l.size() > r.size(); });
    while (nexts[0].size() > 0)
    {
        vector<int> bobo;
        REP(q, nexts.size())
        {
            if (nexts[q].size() == 0)
                break;
            bobo.push_back(nexts[q].back());
            nexts[q].pop_back();
        }
        next_moves.push_back(bobo);
    }
    for (auto x : next_moves)
    {
        REP(q, x.size() - 1)
        {
            swap(inputs[x[q]], inputs[x[q + 1]]);
        }
    }
    REP(i, inputs.size())
    {
        if (i)
            cout << " ";
        cout << inputs[i];
    }
    cout << endl;
}
#undef int

// generated by oj-template v4.7.2
// (https://github.com/online-judge-tools/template-generator)
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout << fixed << setprecision(100);
    int t = 1;
    cin >> t; // comment out if solving multi testcase
    for (int testCase = 1; testCase <= t; ++testCase)
    {
        solve();
    }
    return 0;
}