#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    long long w, n;
    cin >> w;
    while (w--) {
        cin >> n;
        cout << -n+1 << " " <<  n << "\n";
    }
    return 0;
}