#line 1 "Contests/Codeforces_1530/E/main.cpp"
using namespace std;
#line 2 "library/bits/stdc++.h"

// C
#ifndef _GLIBCXX_NO_ASSERT
#include <cassert>
#endif
#include <cctype>
#include <cerrno>
#include <cfloat>
#include <ciso646>
#include <climits>
#include <clocale>
#include <cmath>
#include <csetjmp>
#include <csignal>
#include <cstdarg>
#include <cstddef>
#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <ctime>

#if __cplusplus >= 201103L
#include <ccomplex>
#include <cfenv>
#include <cinttypes>
#include <cstdbool>
#include <cstdint>
#include <ctgmath>
#include <cwchar>
#include <cwctype>
#endif

// C++
#include <algorithm>
#include <bitset>
#include <complex>
#include <deque>
#include <exception>
#include <fstream>
#include <functional>
#include <iomanip>
#include <ios>
#include <iosfwd>
#include <iostream>
#include <istream>
#include <iterator>
#include <limits>
#include <list>
#include <locale>
#include <map>
#include <memory>
#include <new>
#include <numeric>
#include <ostream>
#include <queue>
#include <set>
#include <sstream>
#include <stack>
#include <stdexcept>
#include <streambuf>
#include <string>
#include <typeinfo>
#include <utility>
#include <valarray>
#include <vector>

#if __cplusplus >= 201103L
#include <array>
#include <atomic>
#include <chrono>
#include <condition_variable>
#include <forward_list>
#include <future>
#include <initializer_list>
#include <mutex>
#include <random>
#include <ratio>
#include <regex>
#include <scoped_allocator>
#include <system_error>
#include <thread>
#include <tuple>
#include <typeindex>
#include <type_traits>
#include <unordered_map>
#include <unordered_set>
#endif
#line 3 "Contests/Codeforces_1530/E/main.cpp"

// add your library with double quotation #include"" here.

// add your library with double quotation #include"" here.
// define your macros here.
using namespace std;
typedef string::const_iterator State;
#define eps 1e-8L
#define MAX_MOD 1000000007LL
#define GYAKU 500000004LL
#define MOD 998244353LL
#define pb push_back
#define mp make_pair
typedef long long ll;
typedef long double ld;
#define REP(a,b) for(long long (a) = 0;(a) < (b);++(a))
#define ALL(x) (x).begin(),(x).end()

#define int long long

void solve(){
  string s;
  cin >> s;
  int cnt[26] = {};
  REP(i,s.length()){
    cnt[s[i] - 'a']++;
  }
  vector<pair<int,char>> inputs;
  REP(i,26){
    if(cnt[i]){
      inputs.push_back(mp(cnt[i],i + 'a'));
    }
  }
  
  if(inputs.size() == 1){
    // obvious.
    REP(i,inputs[0].first){
      cout << inputs[0].second;
    }
    cout << endl;
    return;
  }


  sort(ALL(inputs),[](auto a,auto b){
    return a.second < b.second;
    }
  );

  int ok = 0;
  REP(i,inputs.size()){
    if(inputs[i].first == 1) ok = 1;
  }
  if(ok == 1){
    // ans = 0
    REP(i,inputs.size()){
      if(inputs[i].first == 1){
        cout << inputs[i].second;
        inputs[i].first--;
        break;
      }
    }
    for(int i = 0;i < inputs.size();++i){
      REP(q,inputs[i].first){
        cout << inputs[i].second;
      }
    }
    cout << endl;
    return;
  }

  // ans = 1
  // check if aababab... type
  {
    string tmp;
    vector<pair<int,char>> memo_in = inputs;
    tmp.push_back(inputs[0].second);
    tmp.push_back(inputs[0].second);
    inputs[0].first -= 2;
    int itr = 1;
    while(inputs[0].first != 0){
      while(itr < inputs.size() and inputs[itr].first == 0){
        itr++;
      }
      if(itr == inputs.size()) break;
      tmp.push_back(inputs[itr].second);
      tmp.push_back(inputs[0].second);
      inputs[0].first--;
      inputs[itr].first--;
    }
    if(inputs[0].first == 0){
      REP(i,inputs.size()){
        REP(q,inputs[i].first){
          tmp.push_back(inputs[i].second);
        }
      }
      cout << tmp << endl;
      return;
    }
    inputs = memo_in;
  }
  if(inputs.size() == 2){
    cout << inputs[0].second;
    inputs[0].first--;
    for(int i = inputs.size()-1;i >= 0;--i){
      REP(q,inputs[i].first){
        cout << inputs[i].second;
      }
    }
    cout << endl;
    return;
  }

  // abaaaaa ... c b type
  cout << inputs[0].second;
  cout << inputs[1].second;
  inputs[0].first--;
  inputs[1].first--;
  REP(i,inputs[0].first){
    cout << inputs[0].second;
  }
  inputs[0].first = 0;
  cout << inputs[2].second;
  inputs[2].first--;
  REP(i,inputs.size()){
    REP(q,inputs[i].first){
      cout << inputs[i].second;
    }
  }
  cout << endl;
  return;
}
#undef int

// generated by oj-template v4.7.2 (https://github.com/online-judge-tools/template-generator)
int main() {
    // Fasterize input/output script
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout << fixed << setprecision(100);
    // scanf/printf user should delete this fasterize input/output script

    int t = 1;
    cin >> t; // comment out if solving multi testcase
    for(int testCase = 1;testCase <= t;++testCase){
        solve();
    }
    return 0;
}