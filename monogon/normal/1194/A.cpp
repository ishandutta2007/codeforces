
#include <bits/stdc++.h>

#define ll long long
using namespace std;

int q;
ll n, x;

int main() {
    ios::sync_with_stdio(false);
    cin >> q;
    while(q--) {
        cin >> n >> x;
        cout << (2 * x) << endl;
    }
}