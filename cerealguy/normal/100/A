int main() {
  string s=Stdio.stdin->gets();
  int n,k,n1;
  sscanf(s,"%d %d %d", n, k, n1);
 
  if (k >= ((n+n1-1)/n1) * ((n+n1-1)/n1)) {
     write("YES");
  } else {
     write("NO"); 
  }
 
  return 0;
}