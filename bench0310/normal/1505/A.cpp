#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    string s;
    while(getline(cin,s))
    {
        cout << "NO\n";
        cout.flush();
    }
    return 0;
}